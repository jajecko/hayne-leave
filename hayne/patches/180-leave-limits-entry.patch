--- a/legacy/application/views/menu/index.php
+++ b/legacy/application/views/menu/index.php
@@ -60,4 +60,5 @@
                 <li class="nav-header">Urlopy</li>
                 <li><a href="<?php echo base_url(); ?>leavetypes">Rodzaje nieobecności</a></li>
+                <li><a href="<?php echo base_url(); ?>haynelimits">Limity urlopowe</a></li>
                 <li class="divider"></li>
                 <li class="nav-header">System</li>
--- a/legacy/application/controllers/Entitleddays.php
+++ b/legacy/application/controllers/Entitleddays.php
@@ -149,4 +149,8 @@
             $days = $this->input->post('days', TRUE);
             $type = $this->input->post('type', TRUE);
             $description = sanitize($this->input->post('description', TRUE));
+            if (filter_var($days, FILTER_VALIDATE_INT) === FALSE) {
+                $this->output->set_header("HTTP/1.1 422 Unprocessable entity");
+                return;
+            }
             if (isset($startdate) && isset($enddate) && isset($days) && isset($type) && isset($user_id)) {
@@ -173,4 +177,8 @@
             $days = $this->input->post('days', TRUE);
             $type = $this->input->post('type', TRUE);
             $description = sanitize($this->input->post('description', TRUE));
+            if (filter_var($days, FILTER_VALIDATE_INT) === FALSE) {
+                $this->output->set_header("HTTP/1.1 422 Unprocessable entity");
+                return;
+            }
             if (isset($startdate) && isset($enddate) && isset($days) && isset($type) && isset($contract_id)) {
@@ -202,4 +210,8 @@
             if (isset($id) && isset($operation)) {
                 $this->output->set_content_type('text/plain');
                 $days = $this->input->post('days', TRUE);
+                if ($days !== NULL && filter_var($days, FILTER_VALIDATE_INT) === FALSE) {
+                    $this->output->set_header("HTTP/1.1 422 Unprocessable entity");
+                    return;
+                }
                 switch ($operation) {
